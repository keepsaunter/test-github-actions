name: dispatch-test711
on:
  workflow_dispatch:
    inputs:
      input1:
        description: this is des7711
        required: true
        type: string
jobs:
  job1:
    run-on: ubuntu-latest
    steps:
      - run: echo ${{inputs.input1}}-test