on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          mkdir tes5
          echo "new content" > tes5/t3.js
      - uses: actions/upload-artifact@v3
        with:
          name: my-artifact2123
          path: tes5
      - run: ls tes5
  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - uses: actions/download-artifact@v3
        with:
          name: my-artifact2123
          path: tes9
      - run: ls
      - run: ls tes9