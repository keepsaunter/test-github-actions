name: test push when call
on:
  push
jobs:
  job1:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/myaction
        id: my_action
      - run: echo ${{steps.my_action.outputs}}--output
      - run: echo test2
