name: test workflow_call call
on:
  workflow_call:
    inputs:
      test:
        type: string
    outputs:
      outputVar11: ${{steps.step1-2.outputs.step1-2}}
jobs:
  job1:
    runs-on: ubuntu-18.04
    # outputs:
    #   outputVar1
    steps:
      - run: echo ttt6${{inputs.test}}
      - id: step1-2
        run: echo "::set-output name=outputVar1::test-output222666"